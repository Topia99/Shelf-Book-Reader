# CHANGELOG

## v0.2.1 - 2026-07-05

### 修复
- 双击取词完全无响应：全局 `user-select: none` 连带禁掉了 PDF 文字层的双击选词，
  导致选区始终为空。文字层单独放开 `user-select: text` 后恢复正常
- 查词请求失败时给出 toast 提示（原为静默失败）

## v0.2.0 - 2026-07-05

### 新增：单词即时翻译（取词弹窗）
- 阅读英文 PDF 时双击单词，原地弹出翻译卡片：单词、音标、中文释义（按词性分行，
  超 5 条折叠）、发音按钮（系统合成语音，离线）
- 变形词自动还原（running → run、went → go），标注原形
- 内置离线词典：ECDICT 裁剪版 36.6 万词条 + 5.5 万词形反查，dict.db 23.8 MB，
  作为只读资源随安装包分发，与业务库 library.db 物理分离
- 边界处理：首尾标点自动剥离、大小写归一、中文/数字/多词不响应、
  生僻词显示"未找到释义"、扫描版 PDF 提示不支持（每次会话仅提示一次）、
  无英文语音时隐藏发音按钮
- 关闭方式：✕ / 点击外部 / Esc / 翻页缩放滚动自动关闭；新弹窗替换旧弹窗
- 技术：PDF 页面新增透明文字层（PDF.js TextLayer）承载双击选词；
  点击翻页区改为基于点击位置判定（文字上的点击留给选词，不触发翻页）

## v0.1.0 - MVP

### 2026-07-04（下午）
- 修复：asset 协议 403 导致无法打开 PDF/显示封面（改为启动时用运行时 API 将
  books/covers 目录加入白名单，不依赖配置 glob）
- 阅读器打开时自动补生成缺失的封面与总页数
- NSIS 安装包：当前用户级安装（免管理员）、中文安装界面、自动创建桌面图标
- README（安装、快捷键、FAQ）；代码推送 GitHub

### 2026-07-04
- 项目初始化：Tauri 2 + React + TypeScript + PDF.js + SQLite（rusqlite）
- T1 项目骨架：SQLite 初始化（WAL 模式）、dialog 插件、asset 协议（大文件 Range 读取）
- T2 添加书籍：文件选择/拖拽入库、SHA-256 查重、复制到 %APPDATA%/Shelf/books、
  PDF.js 提取元数据书名与首页封面、加密/损坏 PDF 降级为默认封面
- T3 书库页：封面网格、进度显示（页码/百分比）、排序（最近阅读/添加时间/书名）、
  书名搜索、右键菜单（打开/重命名/移除，移除带二次确认）
- T4 阅读器：单页/双页渲染、适合页宽/适合整页/Ctrl+滚轮缩放、目录侧边栏（outline）、
  页码跳转、键盘/滚轮/点击翻页区翻页、加密 PDF 密码输入
- T5 阅读进度：翻页 1 秒节流写库、退出/关闭强制落盘、打开自动跳回上次页码

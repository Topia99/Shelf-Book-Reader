# 跨平台假设审计清单（P0-2，2026-07-08）

审计范围：v0.2.2 全仓库。阻塞/重要/次要三级。触屏交互与 CSS 条目由阶段 4（P4-4/P4-5）处理，配置级条目由 P0-4 处理。

**阻塞**
- [src-tauri/src/lib.rs](/Users/jasonzeng/Developer/shelf-book-reader/src-tauri/src/lib.rs:515)｜启动时直接读取 `APPDATA` 并固定写入 `APPDATA/Shelf`；macOS/iOS 没有这个环境变量，`setup` 会直接 panic｜两者｜改用 `app.path().app_data_dir()` 或 `BaseDirectory::AppData` 统一解析数据目录｜阻塞
- [src-tauri/tauri.conf.json](/Users/jasonzeng/Developer/shelf-book-reader/src-tauri/tauri.conf.json:33)｜`bundle.targets` 只有 `nsis`，当前只能产出 Windows 安装包，macOS 打包链路缺失｜macOS｜补充 `dmg`/`app` 等 macOS 目标，并单独走 iOS 的 mobile build 流程｜阻塞
- [src/components/Library.tsx](/Users/jasonzeng/Developer/shelf-book-reader/src/components/Library.tsx:230)｜书籍打开入口只有 `onDoubleClick`（移动端：不可用）；iOS 上导入后没有单击/轻触打开路径｜iOS｜给书卡补 `onClick`/轻触打开，双击只作为桌面快捷方式｜阻塞
- [src/components/Library.tsx](/Users/jasonzeng/Developer/shelf-book-reader/src/components/Library.tsx:231)｜重命名/移除入口只挂在 `onContextMenu` 右键菜单上（移动端：不可用）｜iOS｜把操作入口改成显式按钮、长按菜单或卡片内更多按钮｜阻塞

**重要**
- [src-tauri/tauri.conf.json](/Users/jasonzeng/Developer/shelf-book-reader/src-tauri/tauri.conf.json:27)｜`assetProtocol.scope` 硬编码为 `$DATA/Shelf/**`；这和 Rust 侧手工拼 `%APPDATA%/Shelf` 绑定在一起，只在 Windows 目录模型下天然对齐｜两者｜让资源白名单与实际数据目录都基于同一套 Tauri path API 生成，或仅保留运行时 `allow_directory`｜重要
- [src-tauri/tauri.conf.json](/Users/jasonzeng/Developer/shelf-book-reader/src-tauri/tauri.conf.json:34)｜`bundle.icon` 只声明了 `.ico` 和 `.png`，仓库虽有 `icon.icns`，但 macOS bundle 不会使用它｜macOS｜把 `icons/icon.icns` 加入 `bundle.icon`｜重要
- [src-tauri/tauri.conf.json](/Users/jasonzeng/Developer/shelf-book-reader/src-tauri/tauri.conf.json:13)｜窗口配置完全按桌面假设：`1240x820`、`minWidth: 720`、`minHeight: 480`、`dragDropEnabled: true`；移动端没有对应适配策略｜iOS｜拆分桌面/移动配置，并把拖放、最小窗口尺寸等桌面能力做条件化处理｜重要
- [src/components/Library.tsx](/Users/jasonzeng/Developer/shelf-book-reader/src/components/Library.tsx:129)｜入库支持 `onDragDropEvent` 拖放（移动端：不可用）；虽然有“添加书籍”按钮，但拖放被当作主路径之一｜iOS｜把文件选择器作为主入口，拖放仅保留为桌面增强能力｜重要
- [src/components/Reader.tsx](/Users/jasonzeng/Developer/shelf-book-reader/src/components/Reader.tsx:231)｜取词入口只有双击选词 `onDoubleClick`（移动端：基本不可用）；iOS 的文本选择和双击语义都不稳定｜iOS｜增加长按选词后“查词”动作，或提供显式取词模式｜重要
- [src/components/Reader.tsx](/Users/jasonzeng/Developer/shelf-book-reader/src/components/Reader.tsx:149)｜阅读器大量依赖键盘快捷键翻页/返回/提交（移动端：仅外接键盘可用）｜iOS｜保证所有核心动作都有可触摸控件，不把键盘当主入口｜重要
- [src/components/Reader.tsx](/Users/jasonzeng/Developer/shelf-book-reader/src/components/Reader.tsx:183)｜缩放和边缘翻页依赖滚轮事件与修饰键（移动端：不可用）｜iOS｜补充双指缩放或显式触控缩放控件，避免依赖 `wheel`｜重要
- [src/styles.css](/Users/jasonzeng/Developer/shelf-book-reader/src/styles.css:75)｜CSS Top 5 #1：书库工具栏是单行桌面布局，`gap/padding/max-width` 固定，窄屏下搜索框、排序、添加按钮会互相挤压｜iOS｜给工具栏加换行或移动端纵向布局｜重要
- [src/styles.css](/Users/jasonzeng/Developer/shelf-book-reader/src/styles.css:305)｜CSS Top 5 #2：阅读器工具栏强制 `flex-wrap: nowrap`，包含多组按钮和页码输入，小屏必然溢出｜iOS｜移动端改为两行工具栏或折叠次要操作｜重要
- [src/styles.css](/Users/jasonzeng/Developer/shelf-book-reader/src/styles.css:242)｜CSS Top 5 #3：弹窗固定 `width: 380px`，在窄屏手机上接近或超过可用宽度｜iOS｜改成 `min(380px, calc(100vw - 24px))` 一类自适应宽度｜重要
- [src/styles.css](/Users/jasonzeng/Developer/shelf-book-reader/src/styles.css:361)｜CSS Top 5 #4：目录侧栏固定 `width: 260px`，手机横向空间会被侧栏大量吞掉｜iOS｜移动端把目录改为抽屉/覆盖层｜重要
- [src/styles.css](/Users/jasonzeng/Developer/shelf-book-reader/src/styles.css:407)｜CSS Top 5 #5：页面舞台固定 `padding: 24px` 且 `width: max-content`，小屏容易造成额外横向滚动和可点击区域过窄｜iOS｜缩小内边距并为移动端使用 `width: 100%` 的单页布局｜重要

**次要**
- [src/platform.ts](/Users/jasonzeng/Developer/shelf-book-reader/src/platform.ts:2)｜用 `navigator.userAgent` 里的 `/Mac/i` 判定平台，属于脆弱 UA 假设，对 iOS/macOS 分流不可靠｜两者｜改用 Tauri 平台 API 或直接做功能检测｜次要
- [src/styles.css](/Users/jasonzeng/Developer/shelf-book-reader/src/styles.css:42)｜多个交互只定义了 `:hover` 反馈（同文件 54、128、225、388、511、524；移动端：可用但无反馈）｜iOS｜为触屏补 `:active`/显式按下态，不依赖 hover 传达可点击性｜次要
- [src/components/Library.tsx](/Users/jasonzeng/Developer/shelf-book-reader/src/components/Library.tsx:245)｜拖放提示文案写死“松开鼠标”，带有桌面鼠标假设｜iOS｜改成中性文案，或仅在桌面拖放时显示｜次要

补充扫描结论：
- 未发现硬编码反斜杠、盘符路径或 `%APPDATA%` 之外的 Windows 环境变量命中。
- 未发现 Windows 专属 Rust API / crate 条件编译命中，也未发现 `std::process::Command` / `Command::new` 调用。
